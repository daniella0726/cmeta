json.extract! estudiante, :id, :nombre, :semestre, :carrera, :universidad, :trabaja, :empresa, :cargo, :encuestador, :supervisor, :revisor, :created_at, :updated_at
json.url estudiante_url(estudiante, format: :json)
